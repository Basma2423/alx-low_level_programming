#include "main.h"

/**
 * _abs - absolute value
 *
 * @a: parameter will be operated on
 *
 * Description: wade7 gedan
 *
 * Return: abs(num)
 */
int _abs(int a)
{
	if (a < 0)
	{
		a = -a;
		return (a);
	}
	else
		return (a);
}
